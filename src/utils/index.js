const constants = {
  images: require('./images/images'),
  fonts: require('./fontFamily/fonts'),
  strings: require('./strings/strings'),
  colors: require('./colors/colors'),
};

module.exports = constants;
